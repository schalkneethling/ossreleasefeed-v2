{
  "name": "worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var GITHUB_PAT:\"$GITHUB_PAT\"",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@hono/standard-validator": "^0.2.0",
    "@octokit/rest": "^22.0.0",
    "@sentry/cloudflare": "^10.64.0",
    "effect": "^3.22.0",
    "feed": "^6.0.0",
    "hono": "^4.9.6",
    "sax": "^1.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
